# Scripts

- `diagnose_ppc930.ps1` / `.sh`: descoberta física do PIN pad.
- `run.ps1` / `.sh`: execução local.
- `run-tests.ps1` / `.sh`: testes unitários.
- `test_mock.py`: smoke test HTTP do fluxo mock.
- `install-windows.ps1`: autostart no Windows.
- `install-linux.sh`: systemd no Linux.
- `status.*`: estado do agente.
